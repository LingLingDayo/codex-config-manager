# Codex 配置助手 (Codex Config Manager) - 项目方向性约束与规则

本文档记录适用于本项目的专有业务架构、交互哲学与工程约束。后续所有在本仓库开展的架构演进、功能新增与重构均须严格遵守以下原则。

---

## 1. 窗口形态与分层交互底线 (Form Factor & View Hierarchy)

本应用是针对 Codex (ChatGPT) 桌面端配置的轻量级辅助工具，**窗口尺寸固定且极为紧凑（520 × 350 px）**。

- **主视口（Main View）纯粹性**：
  - 主界面仅保留核心高频路径：当前生效配置卡片及预设切换入口。
  - 严禁在主视口中塞入低频配置、大段说明或调试状态，严禁无故扩张主卡片高度导致主视口产生全局滚动条。
- **局部扩展配置（80% 抽屉体系）**：
  - 针对当前中转站的次要/扩展配置（如自定义模型参数等），统一采用底部向上滑出的 80% 高度局部抽屉，操作完毕后随手关闭，保障轻快感。
- **全局系统级参数（全屏模态体系）**：
  - 涉及应用本身的全局设定（如客户端可执行文件路径选择、启动前查杀实例等），必须使用 `100vw × 100vh` 的全屏覆盖弹窗，在有限物理窗口内提供无遮挡的沉浸设置空间，关闭后无缝返回主工作流。

---

## 2. 核心概念与命名隔离铁律 (Domain Concepts & Naming Boundaries)

“配置 (Config)”与“设置 (Settings)”在本项目中有严格的领域划分，**绝对禁止概念混淆**：

| 维度 | 配置 (Config) | 设置 (Settings) |
| :--- | :--- | :--- |
| **管理对象** | 被管理的外部应用 (Codex / ChatGPT) 的运行参数 | 本管理工具自身的系统偏好与运行时行为 |
| **涵盖内容** | API Key、Provider URL、自定义 Model、中转站预设 | 客户端安装路径选择、自动拉起时重启行为等 |
| **存储载体** | `~/.codex/config.toml` 与 `~/.codex/auth.json` | `.codex/settings.json`（dev 下为 `settings_dev.json`） |
| **UI 文案** | 「配置」、「更多配置」、「生效配置」 | 「设置」、「系统设置」 |
| **词根约束** | 领域代码强制采用 `config` 语义，严禁出现 `setting` 词根 | 领域代码强制采用 `setting` 语义，严禁混入 `config` 词根 |

**约束要求**：
- 严禁在任何地方将局部抽屉命名为“设置抽屉”或文案标注为“更多设置”。
- 严禁在全局设置页中引用针对单一中转站的临时参数。

---

## 3. 视觉呈现与交互美学准则 (Visual Aesthetics & Interaction)

- **反卡片嵌套原则 (Anti-Card-Nesting)**：
  - 在已经具备独立背景底色与边界的弹窗（Modal）或抽屉（Drawer）中，**严禁再次堆叠带有独立背景色、独立边框（border）的卡片盒子**。
  - 所有表单项统一使用无框平铺的结构，直接融入背景底色，依靠微弱间距与排版建立秩序，杜绝“满屏都是卡片框”的笨重感。
- **面向终端用户的人性化可读性 (Human-Readable UI)**：
  - 任何呈现给用户的检测数据、状态反馈，必须是直观、真实物理现实的信息（如真实的文件绝对路径：`C:\...\app\ChatGPT.exe`）。
  - **严禁直接向界面抛出底层黑话或虚拟外壳标识**（例如绝对不能直接向用户展示 `shell:AppsFolder\...` 等 Windows 内部虚拟标识符）。
- **动效克制与稳重性 (Restrained Motion)**：
  - 动画仅服务于界面的层级变化（抽屉升降、全屏淡入、弹窗缩放）及关键操作的加载反馈。
  - 移除无实质交互意义的装饰性动效（如按钮 hover 时的图标旋转等），确保桌面工具类软件的高级、沉稳质感。

---

## 4. 配置文件保护与系统进程调度 (Config Safety & Process Execution)

- **TOML 配置文件安全性 (Safe TOML Patching)**：
  - 写入目标应用的 `config.toml` 时，必须保留原文件中的用户自定义注释、结构排列与未纳管字段，禁止粗暴的全局序列化覆盖破坏原始格式。
  - 严格保持开发与生产环境隔离：非 release 环境下必须强制读写 `config_dev.toml`、`auth_dev.json` 与 `settings_dev.json`。
- **跨平台进程拉起安全 (Process Lifecycle Safety)**：
  - 启动外部应用时，必须使用非阻塞且脱离父进程的模式（如 Windows 下使用 `DETACHED_PROCESS` 或外壳托管），严禁产生黑框控制台残留，严禁阻塞 Tauri 主事件循环。
  - 终止旧实例后必须执行进程销毁与互斥锁释放的轮询探测（建议最长 1.5 秒，100ms 探测周期），避免因为单实例锁未释放导致新实例拉起静默退出。
  - 路径探测需具备动态检测能力：优先探测当前活跃运行中的进程路径，其次解析系统真实物理安装目录（包括 Windows Store 应用的真实 `InstallLocation`），向前端返回标准的绝对可执行文件路径。

---

## 5. 组件与扩展规范 (Component Architecture)

- **通用控件复用**：
  - 所有设置类交互项须抽离为通用组件（存放于 `src/components/settings/` 目录下），后续新增设置项时优先组合复用，保持视觉与行为统一。
- **高内聚自治**：
  - 主配置卡片仅负责当前生效配置的表单渲染与中转联动，全局级状态统一由各自专属的 Composable 进行状态切片与生命周期管理，避免全局状态与局部卡片强耦合。
